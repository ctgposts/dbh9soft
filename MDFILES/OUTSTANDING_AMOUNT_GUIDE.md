# Outstanding Amount Management Module 🏦
## সম্পূর্ণ কাস্টমার বাকি আদায় ব্যবস্থাপনা

### 📋 ওভারভিউ (Overview)
এটি একটি সম্পূর্ণ ৩৬০ ডিগ্রী আউটস্ট্যান্ডিং অ্যামাউন্ট ম্যানেজমেন্ট সিস্টেম যা কাস্টমারদের কাছ থেকে বাকি টাকা সংগ্রহের সম্পূর্ণ প্রক্রিয়া পরিচালনা করে।

---

## 🎯 মূল ফিচার (Key Features)

### 1. **আউটস্ট্যান্ডিং অ্যামাউন্ট ম্যানেজমেন্ট**
   - নতুন বাকি রেকর্ড তৈরি
   - কাস্টমার উইজ ট্র্যাকিং
   - স্বয়ংক্রিয় স্ট্যাটাস আপডেট (Active, Partial, Overdue, Resolved)
   - মাল্টি-ব্র্যান্চ সাপোর্ট

### 2. **পেমেন্ট রেকর্ডিং**
   - একাধিক পেমেন্ট মেথড সাপোর্ট:
     - ক্যাশ (Cash)
     - ক্রেডিট/ডেবিট কার্ড (Card)
     - ব্যাংক ট্রান্সফার (Bank Transfer)
     - মোবাইল ব্যাংকিং (Mobile Banking - bKash, Nagad, Rocket ইত্যাদি)
     - চেক (Cheque)
     - অন্যান্য (Other)
   - পেমেন্ট হিস্টরি ট্র্যাকিং
   - ট্রানজ্যাকশন আইডি রেকর্ডিং

### 3. **কালেকশন ফলো-আপ সিস্টেম**
   - একাধিক ফলো-আপ টাইপ:
     - ফোন কল (Phone Call)
     - এসএমএস (SMS)
     - ইমেইল (Email)
     - হোয়াটসঅ্যাপ (WhatsApp)
     - ফিজিক্যাল ভিজিট (Physical Visit)
     - নোট (Note)
   - ফলো-আপ আউটকাম ট্র্যাকিং:
     - পেমেন্ট প্রতিশ্রুত (Promised)
     - আংশিক পেমেন্ট (Partial Paid)
     - প্রতিক্রিয়া পেন্ডিং (Pending Response)
     - কোন প্রতিক্রিয়া না (No Response)
     - পেমেন্ট অস্বীকার (Refused to Pay)
   - প্রতিশ্রুত পেমেন্ট ও তারিখ রেকর্ডিং
   - পরবর্তী ফলো-আপ সিডিউল করা

### 4. **পেমেন্ট রিমাইন্ডার সিস্টেম**
   - স্বয়ংক্রিয় রিমাইন্ডার সিডিউলিং
   - একাধিক রিমাইন্ডার চ্যানেল:
     - SMS
     - Email
     - WhatsApp
     - Phone Call
   - ওভারডিউ ডেজ ট্র্যাকিং
   - রিমাইন্ডার স্ট্যাটাস মনিটরিং

### 5. **এজিং রিপোর্ট**
   - স্বয়ংক্রিয় এজিং ক্যালকুলেশন:
     - 0-30 দিন (Current)
     - 30-60 দিন
     - 60-90 দিন
     - 90+ দিন (Overdue)
   - কাস্টমার-ওয়াইজ এজিং বিশ্লেষণ
   - রিপোর্ট এক্সপোর্ট সুবিধা

### 6. **ড্যাশবোর্ড ও সামারি**
   - টোটাল আউটস্ট্যান্ডিং অ্যামাউন্ট
   - ওভারডিউ অ্যামাউন্ট
   - রিজলভড অ্যামাউন্ট
   - একটিভ কাস্টমার কাউন্ট
   - স্ট্যাটাস-ওয়াইজ ব্রেকডাউন

### 7. **সার্চ ও ফিল্টারিং**
   - কাস্টমার নেম বাই সার্চ
   - ফোন নম্বর বাই সার্চ
   - স্ট্যাটাস ফিল্টার
   - সর্টিং অপশন:
     - অ্যামাউন্ট (High to Low)
     - আউটস্ট্যান্ডিং ডেজ
     - কাস্টমার নেম
     - রিসেন্টলি আপডেটেড

---

## 📊 ডেটাবেস স্কিমা (Database Schema)

### 1. **outstandingAmounts Table**
```
- customerId: Customer reference
- customerName: String
- customerPhone: String (optional)
- customerEmail: String (optional)
- branchId: Branch reference
- branchName: String
- saleIds: Array of sale references
- totalAmount: Number (মোট বাকি অ্যামাউন্ট)
- paidAmount: Number (এ পর্যন্ত পরিশোধিত অ্যামাউন্ট)
- remainingAmount: Number (বাকি বালেন্স)
- outstandingDays: Number (কতদিন ধরে বাকি)
- status: String (active, resolved, overdue, partial)
- dueDate: Number (optional - প্রত্যাশিত পেমেন্ট তারিখ)
- notes: String (optional)
- createdAt: Number
- updatedAt: Number
- createdBy: User reference
- lastModifiedBy: User reference
```

### 2. **outstandingPayments Table**
```
- outstandingId: Reference to outstanding amount
- customerId: Customer reference
- saleId: Sale reference (optional)
- amount: Number (পেমেন্ট অ্যামাউন্ট)
- paymentMethod: String (cash, card, bank_transfer, mobile_banking, cheque, other)
- paymentDetails: Object
  - transactionId: String (optional)
  - phoneNumber: String (optional - মোবাইল ব্যাংকিং নম্বর)
  - chequeNumber: String (optional)
  - bankName: String (optional)
  - reference: String (optional)
- paymentDate: Number
- notes: String (optional)
- branchId: Branch reference
- recordedBy: User reference
- recordedByName: String
```

### 3. **collectionFollowups Table**
```
- outstandingId: Reference to outstanding amount
- customerId: Customer reference
- followupType: String (call, sms, email, whatsapp, visit, note)
- description: String (ফলো-আপের বর্ণনা)
- promiseAmount: Number (optional - কাস্টমার যে পরিমাণ পেমেন্ট করতে প্রতিশ্রুত)
- promiseDate: Number (optional - প্রতিশ্রুত পেমেন্ট তারিখ)
- outcome: String (promised, partial_paid, pending, no_response, refused)
- notes: String (optional)
- nextFollowupDate: Number (optional)
- createdAt: Number
- createdBy: User reference
- createdByName: String
```

### 4. **paymentReminders Table**
```
- outstandingId: Reference to outstanding amount
- customerId: Customer reference
- reminderType: String (sms, email, whatsapp, call)
- reminderStatus: String (pending, sent, failed, acknowledged)
- message: String (optional)
- scheduledFor: Number (রিমাইন্ডার পাঠানোর সময়)
- daysOverdue: Number
- sentAt: Number (optional)
- sentBy: User reference (optional)
- failureReason: String (optional)
- createdAt: Number
```

### 5. **outstandingAging Table**
```
- customerId: Customer reference
- customerName: String
- branchId: Branch reference
- current: Number (0-30 দিন)
- days30_60: Number (30-60 দিন)
- days60_90: Number (60-90 দিন)
- days90plus: Number (90+ দিন - Overdue)
- totalOutstanding: Number
- lastUpdated: Number
```

---

## 🔧 API Functions (Convex)

### Query Functions

#### 1. **list** - সব আউটস্ট্যান্ডিং রেকর্ড লিস্ট
```javascript
const outstanding = await query(api.outstanding.list, {
  customerId: optional,
  branchId: optional,
  status: optional,
  searchTerm: optional,
  sortBy: optional // "amount", "days", "name", "recent"
});
```

#### 2. **getById** - নির্দিষ্ট রেকর্ড পান
```javascript
const record = await query(api.outstanding.getById, {
  id: "outstanding_id"
});
```

#### 3. **getByCustomerId** - একটি কাস্টমারের সব রেকর্ড
```javascript
const records = await query(api.outstanding.getByCustomerId, {
  customerId: "customer_id"
});
```

#### 4. **getOutstandingSummary** - সামারি ড্যাশবোর্ড
```javascript
const summary = await query(api.outstanding.getOutstandingSummary, {
  branchId: optional
});
// Returns: totalOutstanding, totalResolved, totalOverdue, activeCustomers, etc.
```

#### 5. **getAging** - এজিং রিপোর্ট
```javascript
const aging = await query(api.outstanding.getAging, {
  branchId: optional
});
```

#### 6. **getPaymentHistory** - পেমেন্ট হিস্টরি
```javascript
const payments = await query(api.outstanding.getPaymentHistory, {
  outstandingId: "id"
});
```

#### 7. **getFollowupHistory** - ফলো-আপ হিস্টরি
```javascript
const followups = await query(api.outstanding.getFollowupHistory, {
  outstandingId: "id"
});
```

#### 8. **getReminderHistory** - রিমাইন্ডার হিস্টরি
```javascript
const reminders = await query(api.outstanding.getReminderHistory, {
  outstandingId: "id"
});
```

### Mutation Functions

#### 1. **create** - নতুন আউটস্ট্যান্ডিং রেকর্ড তৈরি
```javascript
await mutation(api.outstanding.create, {
  customerId: "id",
  customerName: "নাম",
  customerPhone: "ফোন",
  customerEmail: "ইমেইল",
  branchId: "branch_id",
  branchName: "শাখার নাম",
  saleIds: [],
  totalAmount: 5000,
  dueDate: timestamp,
  notes: "টীকা"
});
```

#### 2. **addPayment** - পেমেন্ট রেকর্ড করুন
```javascript
await mutation(api.outstanding.addPayment, {
  outstandingId: "id",
  amount: 2000,
  paymentMethod: "cash",
  paymentDetails: {
    transactionId: "TXN123",
    phoneNumber: "+880123456789"
  },
  notes: "পেমেন্ট নোট"
});
```

#### 3. **addFollowup** - ফলো-আপ যোগ করুন
```javascript
await mutation(api.outstanding.addFollowup, {
  outstandingId: "id",
  followupType: "call",
  description: "কাস্টমারকে কল করা হয়েছে",
  promiseAmount: 1000,
  promiseDate: timestamp,
  outcome: "promised",
  nextFollowupDate: timestamp
});
```

#### 4. **scheduleReminder** - রিমাইন্ডার সিডিউল করুন
```javascript
await mutation(api.outstanding.scheduleReminder, {
  outstandingId: "id",
  reminderType: "sms",
  scheduledFor: timestamp,
  message: "বাকি টাকা পরিশোধ করুন"
});
```

#### 5. **updateStatus** - স্ট্যাটাস আপডেট করুন
```javascript
await mutation(api.outstanding.updateStatus, {
  id: "id",
  status: "resolved"
});
```

#### 6. **updateNotes** - নোট আপডেট করুন
```javascript
await mutation(api.outstanding.updateNotes, {
  id: "id",
  notes: "নতুন নোট"
});
```

#### 7. **updateAging** - এজিং তথ্য আপডেট করুন
```javascript
await mutation(api.outstanding.updateAging, {
  outstandingId: "id"
});
```

#### 8. **deleteOutstanding** - রেকর্ড ডিলিট করুন
```javascript
await mutation(api.outstanding.deleteOutstanding, {
  id: "id"
});
```

---

## 🖥️ UI/UX Components

### Main Tabs

#### 1. **Outstanding List Tab**
   - Summary cards (Total Outstanding, Overdue, Resolved, Partial)
   - Search & Filter panel
   - Outstanding records table
   - Action: View Details

#### 2. **Add Outstanding Tab**
   - Form to create new outstanding record
   - Customer name (required)
   - Phone number
   - Email
   - Total amount (required)
   - Due date
   - Notes

#### 3. **Detail View**
   - Outstanding summary with amounts
   - Record Payment button
   - Add Followup button
   - Delete button
   - Payment History
   - Followup History
   - Reminder History

#### 4. **Reports & Aging Tab**
   - Aging report table (0-30, 30-60, 60-90, 90+ days)
   - Summary statistics
   - Status breakdown
   - Amount summary

---

## ✨ কী অনন্য বৈশিষ্ট্য

1. **স্বয়ংক্রিয় স্ট্যাটাস ম্যানেজমেন্ট** - আউটস্ট্যান্ডিং পেমেন্ট স্বয়ংক্রিয়ভাবে আপডেট হয়
2. **মাল্টি-মেথড পেমেন্ট ট্র্যাকিং** - সব ধরনের পেমেন্ট পদ্ধতি সাপোর্ট
3. **কমপ্রিহেন্সিভ ফলো-আপ সিস্টেম** - প্রতিটি কাস্টমার ইন্টারঅ্যাকশন রেকর্ড করুন
4. **এজিং অ্যানালিসিস** - ওভারডিউ টাকা সনাক্ত করুন
5. **রিমাইন্ডার সিস্টেম** - সময়মত কাস্টমারদের জানান
6. **কমপ্লিট হিস্টরি** - পেমেন্ট, ফলো-আপ, রিমাইন্ডার সবকিছুর হিস্টরি
7. **মাল্টি-ব্র্যান্চ সাপোর্ট** - একাধিক শাখা পরিচালনা করুন

---

## 🔄 ওয়ার্কফ্লো এক্সাম্পল (Workflow Example)

### Scenario: নতুন কাস্টমার বাকি পেমেন্ট

1. **রেকর্ড তৈরি**
   - "অ্যাড আউটস্ট্যান্ডিং" ট্যাবে যান
   - কাস্টমার তথ্য এবং বাকি অ্যামাউন্ট এন্টার করুন
   - "সেভ আউটস্ট্যান্ডিং" ক্লিক করুন

2. **পেমেন্ট ট্র্যাক করুন**
   - "অুটস্ট্যান্ডিং লিস্ট" টেবিলে রেকর্ড দেখুন
   - "ডিটেইলস" ক্লিক করুন
   - "রেকর্ড পেমেন্ট" বাটন ক্লিক করুন
   - পেমেন্ট ডিটেইল এন্টার করুন
   - পেমেন্ট সেভ করুন

3. **ফলো-আপ যোগ করুন**
   - "অ্যাড ফলো-আপ" বাটন ক্লিক করুন
   - ফলো-আপ টাইপ নির্বাচন করুন (কল, এসএমএস, ইত্যাদি)
   - কথোপকথনের বর্ণনা এন্টার করুন
   - প্রতিশ্রুত অ্যামাউন্ট এবং তারিখ (যদি প্রযোজ্য)
   - পরবর্তী ফলো-আপের তারিখ সেট করুন

4. **রিমাইন্ডার সিডিউল করুন**
   - রিমাইন্ডার টাইপ নির্বাচন করুন
   - পাঠানোর তারিখ সেট করুন
   - বার্তা তৈরি করুন
   - সিডিউল করুন

5. **রিপোর্ট দেখুন**
   - "রিপোর্টস & এজিং" ট্যাবে যান
   - এজিং রিপোর্ট দেখুন
   - সামারি স্ট্যাটিস্টিক্স চেক করুন

---

## 📱 মোবাইল এবং ডেস্কটপ সাপোর্ট
- সম্পূর্ণ রেসপন্সিভ ডিজাইন
- মোবাইলে সুবিধাজনক নেভিগেশন
- টাচ-ফ্রেন্ডলি ইন্টারফেস

---

## 🔐 সিকিউরিটি
- User authentication প্রয়োজন
- প্রতিটি অ্যাকশন লগ করা হয়
- ইউজার রেফারেন্স সংরক্ষণ করা হয়
- সংবেদনশীল তথ্য সুরক্ষিত

---

## 📈 পারফরম্যান্স
- এফিশিয়েন্ট ডেটাবেস কোয়েরি ইনডেক্স সহ
- লেজি লোডিং কম্পোনেন্ট
- প্যাজিনেশন এবং সার্চ অপটিমাইজেশন

---

## 🚀 ভবিষ্যত এনহান্সমেন্ট সম্ভাবনা
1. এসএমএস/ইমেইল ইন্টিগ্রেশন
2. হোয়াটসঅ্যাপ মেসেজ অটোমেশন
3. পিডিএফ রিপোর্ট এক্সপোর্ট
4. এক্সেল/সিএসভি ডাউনলোড
5. ডেলিভারি ট্র্যাকিং ইন্টিগ্রেশন
6. কমিশন ক্যালকুলেশন (কালেকশনের উপর)
7. এআই-বেসড কালেকশন প্রেডিকশন
8. গ্রাফ এবং চার্ট ভিজুয়ালাইজেশন

---

## 📝 ব্যবহারকারী নির্দেশিকা

### সাধারণ পরিচালনা
1. নেভিগেশন মেনু থেকে "Outstanding" সিলেক্ট করুন
2. তিনটি মূল ট্যাব আছে: List, Add Outstanding, Reports
3. প্রতিটি ট্যাবে ইনটিউইটিভ ফর্ম এবং টেবিল রয়েছে

### টিপস এবং ট্রিকস
- নিয়মিত ফলো-আপ করুন সর্বোত্তম সংগ্রহের জন্য
- প্রতিশ্রুত পেমেন্টের তারিখ রেকর্ড করুন
- প্রতিটি ইন্টারঅ্যাকশনের নোট যোগ করুন
- এজিং রিপোর্ট নিয়মিত চেক করুন

---

## ⚠️ গুরুত্বপূর্ণ নোট
- প্রথমে এই মডিউলটি সম্পূর্ণভাবে পরীক্ষা করুন
- গিটহাব পুশ করার আগে আপনার অনুমতির জন্য অপেক্ষা করুন
- ডাটা ব্যাকআপ রাখুন

---

**মডিউল তৈরী তারিখ:** জানুয়ারি ৩০, ২০২৬
**সংস্করণ:** ১.০.০
**স্ট্যাটাস:** পরীক্ষা করতে প্রস্তুত ✅
